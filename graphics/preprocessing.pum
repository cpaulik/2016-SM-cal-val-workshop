!include theme.pum
skinparam defaultFontColor #FFFFFF

: Data acquisition;
if (optional) then
  : Resampling or Reprojection;
  : Subsetting (e.g. only land points);
endif
: Data Conversion;
: Data storage (Time Series optimized);

